interface SectionHeaderProps {
    className?: string;
    header: string;
    subHeader: string;
}
declare const SectionHeader: ({ className, header, subHeader }: SectionHeaderProps) => JSX.Element;
export default SectionHeader;
