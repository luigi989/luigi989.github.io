import { default as React } from 'react';
interface HeaderSocialProps {
    label: string;
    url: string;
    icon: React.ReactNode;
}
declare const HeaderSocial: ({ label, url, icon }: HeaderSocialProps) => JSX.Element;
export default HeaderSocial;
