import { Ref } from 'react';
interface ExperienceProps {
    snap: string;
    visibilityRef: Ref<HTMLDivElement>;
}
declare const Experience: ({ visibilityRef, snap }: ExperienceProps) => JSX.Element;
export default Experience;
